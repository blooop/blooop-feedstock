schema_version: 1

package:
  name: pkl
  version: "0.31.1"

source:
  - url: https://github.com/apple/pkl/releases/download/0.31.1/pkl-linux-amd64  # [linux and x86_64]
    sha256: 618f13955d755cafbfe8c9cba1d27635848cd49dbc6abffd398d2751db1231bf  # [linux and x86_64]
  - url: https://github.com/apple/pkl/releases/download/0.31.1/pkl-linux-aarch64  # [linux and aarch64]
    sha256: 7ef10e743daa921fb94ae7bdb9ec6986f362bf250c55814b9ea2aeb13f2d083e  # [linux and aarch64]
  - url: https://github.com/apple/pkl/releases/download/0.31.1/pkl-macos-amd64  # [osx and x86_64]
    sha256: 22123ed4ae4c03afa8c54c69f77f0bec39b0fa0f67b09d6d148e0a376a2a471d  # [osx and x86_64]
  - url: https://github.com/apple/pkl/releases/download/0.31.1/pkl-macos-aarch64  # [osx and arm64]
    sha256: 1b6a5438d9624cd2798a7530721bbbfa27ef72efe5c878a1b6c546c6e7ca0e8f  # [osx and arm64]
  - url: https://github.com/apple/pkl/releases/download/0.31.1/pkl-windows-amd64.exe  # [win]
    sha256: a8834481667325b44c539dbb758d7365a16389070f343c04b639bbe525ede013  # [win]

build:
  number: 0
  script:
    - if: unix
      then:
        - mkdir -p "$PREFIX/bin"
        - |
          case "${target_platform:-}" in
            linux-64)      SRC_FILE="pkl-linux-amd64" ;;
            linux-aarch64) SRC_FILE="pkl-linux-aarch64" ;;
            osx-64)        SRC_FILE="pkl-macos-amd64" ;;
            osx-arm64)     SRC_FILE="pkl-macos-aarch64" ;;
            *) echo "Unsupported target_platform: ${target_platform:-unknown}" && exit 1 ;;
          esac
          cp "$SRC_DIR/$SRC_FILE" "$PREFIX/bin/pkl"
          chmod 755 "$PREFIX/bin/pkl"
    - if: win
      then:
        - if not exist %PREFIX%\Library\bin mkdir %PREFIX%\Library\bin
        - copy %SRC_DIR%\pkl-windows-amd64.exe %PREFIX%\Library\bin\pkl.exe

tests:
  - script:
      - if: unix
        then:
          - which pkl
          - test -x $PREFIX/bin/pkl
          - pkl --version
      - if: win
        then:
          - where pkl
          - pkl --version

about:
  homepage: https://pkl-lang.org
  license: Apache-2.0
  license_family: APACHE
  summary: A configuration as code language with rich validation and tooling
  description: |
    Pkl is a configuration-as-code language designed with built-in validation
    and comprehensive tooling support. It enables developers to define
    configurations programmatically with strong type checking and validation
    capabilities. Pkl can generate configuration in multiple formats including
    JSON, YAML, XML, and property files.
  documentation: https://pkl-lang.org/main/current/
  repository: https://github.com/apple/pkl

extra:
  recipe-maintainers:
    - blooop
