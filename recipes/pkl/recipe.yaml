schema_version: 1

package:
  name: pkl
  version: "0.32.0"

source:
  - url: https://github.com/apple/pkl/releases/download/0.32.0/pkl-linux-amd64  # [linux and x86_64]
    sha256: 15e7e7375c28b8542b3d13fe35bccaeb7b9542114008998708385489885f41e7  # [linux and x86_64]
  - url: https://github.com/apple/pkl/releases/download/0.32.0/pkl-linux-aarch64  # [linux and aarch64]
    sha256: b1ba7ef5dec9287f8f843ce563911eba822fed5789a5baab8cc712615a9dbaf0  # [linux and aarch64]
  - url: https://github.com/apple/pkl/releases/download/0.32.0/pkl-macos-amd64  # [osx and x86_64]
    sha256: 190ad63fec4f81f40e75dba8d6309033dd30e51b3e042db3b846f67c7ab46e3d  # [osx and x86_64]
  - url: https://github.com/apple/pkl/releases/download/0.32.0/pkl-macos-aarch64  # [osx and arm64]
    sha256: fb891856705f3dcb8589c74999e01ded3eeb6b77ad5c6a95c02579a848c13928  # [osx and arm64]
  - url: https://github.com/apple/pkl/releases/download/0.32.0/pkl-windows-amd64.exe  # [win]
    sha256: 61c40e940d5e61a0c54cc6bdef6ff26d44f02aeccd19fa0407125dc617dba030  # [win]

build:
  number: 0
  script:
    - if: unix
      then:
        - mkdir -p "$PREFIX/bin"
        - |
          case "${target_platform:-}" in
            linux-64)      SRC_FILE="pkl-linux-amd64" ;;
            linux-aarch64) SRC_FILE="pkl-linux-aarch64" ;;
            osx-64)        SRC_FILE="pkl-macos-amd64" ;;
            osx-arm64)     SRC_FILE="pkl-macos-aarch64" ;;
            *) echo "Unsupported target_platform: ${target_platform:-unknown}" && exit 1 ;;
          esac
          cp "$SRC_DIR/$SRC_FILE" "$PREFIX/bin/pkl"
          chmod 755 "$PREFIX/bin/pkl"
    - if: win
      then:
        - if not exist %PREFIX%\Library\bin mkdir %PREFIX%\Library\bin
        - copy %SRC_DIR%\pkl-windows-amd64.exe %PREFIX%\Library\bin\pkl.exe

tests:
  - script:
      - if: unix
        then:
          - which pkl
          - test -x $PREFIX/bin/pkl
          - pkl --version
      - if: win
        then:
          - where pkl
          - pkl --version

about:
  homepage: https://pkl-lang.org
  license: Apache-2.0
  license_family: APACHE
  summary: A configuration as code language with rich validation and tooling
  description: |
    Pkl is a configuration-as-code language designed with built-in validation
    and comprehensive tooling support. It enables developers to define
    configurations programmatically with strong type checking and validation
    capabilities. Pkl can generate configuration in multiple formats including
    JSON, YAML, XML, and property files.
  documentation: https://pkl-lang.org/main/current/
  repository: https://github.com/apple/pkl

extra:
  recipe-maintainers:
    - blooop
