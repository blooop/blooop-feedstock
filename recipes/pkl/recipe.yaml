schema_version: 1

package:
  name: pkl
  version: "0.32.1"

source:
  - url: https://github.com/apple/pkl/releases/download/0.32.1/pkl-linux-amd64  # [linux and x86_64]
    sha256: 3180b62da95c0cad1d904e9bb6c5f4a8f9032413c21e53194bb91ff1ee5f3211  # [linux and x86_64]
  - url: https://github.com/apple/pkl/releases/download/0.32.1/pkl-linux-aarch64  # [linux and aarch64]
    sha256: a76d2dd47da435a8f911b0347373f47c7e59ea54fb75ff846d20b8df10dba058  # [linux and aarch64]
  - url: https://github.com/apple/pkl/releases/download/0.32.1/pkl-macos-amd64  # [osx and x86_64]
    sha256: 5b74b903234047960144f66f2cebdd12d267d9b98e8155ed91d2ae5ed27e2d1f  # [osx and x86_64]
  - url: https://github.com/apple/pkl/releases/download/0.32.1/pkl-macos-aarch64  # [osx and arm64]
    sha256: 563eb51c9a20b16a3625464ed745c675ed9750381f2126722696a0d7cac1d9d3  # [osx and arm64]
  - url: https://github.com/apple/pkl/releases/download/0.32.1/pkl-windows-amd64.exe  # [win]
    sha256: 8550a00fcf027335e42c5e2cd553b88e98845408cb1880b3e3d1860caf46d22a  # [win]

build:
  number: 0
  script:
    - if: unix
      then:
        - mkdir -p "$PREFIX/bin"
        - |
          case "${target_platform:-}" in
            linux-64)      SRC_FILE="pkl-linux-amd64" ;;
            linux-aarch64) SRC_FILE="pkl-linux-aarch64" ;;
            osx-64)        SRC_FILE="pkl-macos-amd64" ;;
            osx-arm64)     SRC_FILE="pkl-macos-aarch64" ;;
            *) echo "Unsupported target_platform: ${target_platform:-unknown}" && exit 1 ;;
          esac
          cp "$SRC_DIR/$SRC_FILE" "$PREFIX/bin/pkl"
          chmod 755 "$PREFIX/bin/pkl"
    - if: win
      then:
        - if not exist %PREFIX%\Library\bin mkdir %PREFIX%\Library\bin
        - copy %SRC_DIR%\pkl-windows-amd64.exe %PREFIX%\Library\bin\pkl.exe

tests:
  - script:
      - if: unix
        then:
          - which pkl
          - test -x $PREFIX/bin/pkl
          - pkl --version
      - if: win
        then:
          - where pkl
          - pkl --version

about:
  homepage: https://pkl-lang.org
  license: Apache-2.0
  license_family: APACHE
  summary: A configuration as code language with rich validation and tooling
  description: |
    Pkl is a configuration-as-code language designed with built-in validation
    and comprehensive tooling support. It enables developers to define
    configurations programmatically with strong type checking and validation
    capabilities. Pkl can generate configuration in multiple formats including
    JSON, YAML, XML, and property files.
  documentation: https://pkl-lang.org/main/current/
  repository: https://github.com/apple/pkl

extra:
  recipe-maintainers:
    - blooop
